# Scoped Re-Review Prompt Contract

Verify the prior findings against the correction without reopening the entire task. Perform a read-only review and do not edit files.

## Prior Findings

`[PRIOR_FINDINGS]`

## Fix Report

`[FIX_REPORT]`

## Changed Paths From the Correction

`[CORRECTION_CHANGED_PATHS]`

For each prior finding, return `addressed` or `not addressed` with `path:line` evidence. End with either `All prior findings addressed.` or a precise list of findings that remain open.
